# Installation

Install Fabrica with pnpm:

```bash
pnpm add @rodkisten/fabrica
```

Fabrica publishes ESM, CommonJS, and TypeScript declaration outputs through a
strict package exports map.

```ts
import { css, html } from '@rodkisten/fabrica';
```
